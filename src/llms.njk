---
permalink: /llms.txt
eleventyExcludeFromCollections: true
---
# {{ site.title }}

> {{ site.description }}

## Pages
{% for entry in collections.all %}
- [{{ entry.data.title or entry.url }}]({{ site.url }}{{ entry.url }}){% if entry.data.description %}: {{ entry.data.description }}{% endif %}
{%- endfor %}

## Data
- [Content feed (JSON)]({{ site.url }}/api/content.json): every published item (events, offers, heroes, announcements) in one shape: id, type, slug, title, summary, url, starts_at, ends_at, location, media, metadata.purpose, updated_at
